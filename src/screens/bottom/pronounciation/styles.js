import { StyleSheet } from 'react-native';
import { hp } from '../../../constants';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: hp(8),
  },
  body: {
    alignItems: 'center',
    width: '100%',
  },
});
